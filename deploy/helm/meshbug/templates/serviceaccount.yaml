{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "meshbug.serviceAccountName" . }}
  labels: {{- include "meshbug.labels" . | nindent 4 }}
{{- end -}}
