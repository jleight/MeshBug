apiVersion: v1
kind: Service
metadata:
  name: {{ include "meshbug.fullname" . }}
  labels: {{- include "meshbug.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "meshbug.selectorLabels" . | nindent 4 }}
